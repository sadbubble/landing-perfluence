node_modules
dist
.env
.env.local
.env.*.local
*.log
.DS_Store
.vercel
supabase/.temp

.vercel

# Оригиналы Helvetica Neue (16 файлов, 6,4 МБ) — коммерческий шрифт.
# В сборку не идут: она берёт урезанные woff2 из public/fonts/.
# В репозиторий не кладутся, потому что он публичный: лицензия КТ
# покрывает использование шрифта, но не раздачу исходных файлов.
# Сами файлы остаются на диске.
assets-src/

# Первоисточники заказчика: гайд Казахтелекома по цветам и шрифтам
# и техзадание с ценами и схемой расчёта с дилерами. Материалы КТ и
# Perfluence, в открытый доступ не выносятся. Остаются на диске.
/for landing.pdf
/Тех. задание Лэндинг.docx
