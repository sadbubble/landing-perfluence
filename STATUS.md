# Статус проекта

**Обновлено:** 4 сентября 2026

Лендинг ТОО «Perfluence» — официального партнёра АО «Казахтелеком».
Задача: собирать заявки на подключение интернета и ТВ, с привязкой каждой
заявки к менеджеру (дилеру, блогеру), приведшему клиента.

---

## Коротко

Лендинг **готов, и приём заявок работает**. Заявку принимает форма Qbox,
встроенная в страницу рамкой; менеджер и тариф едут в неё параметрами
адреса. Проверено настоящей заявкой в CRM — см. ниже.

Для запуска не хватает домена от Казахтелекома и согласия на обработку
персональных данных в форме Qbox.

---

## Приём заявок: как это работает

Своей формы у лендинга больше нет. Раньше он собирал данные сам и слал их
на бэкенд; бэкенд заказчик отменил, а вместо него появилась готовая форма
в Qbox.

```
кнопка на карточке тарифа
   → модальное окно с рамкой, внутри форма Qbox
   → человек заполняет и отправляет
   → лид в CRM с менеджером и тарифом
```

Человек при этом **остаётся на лендинге**: адрес в строке браузера не
меняется, параметры не видны.

Из этого решения ушли сразу три вопроса, которые раньше считались
блокирующими: не нужен CORS, не нужен ключ доступа в коде страницы, не
нужен обработчик на сервере КТ.

### Что проверено на бою

Заявка №13571845 от 4 сентября 2026, воронка «тестовая Нурдаулет»:

| поле в карточке лида | значение |
|---|---|
| Менеджер | `test` — из ссылки `?manager=test` |
| ПРодукт | `Bereket 4 SIM (без контракта)` |
| Имя, Номер, Город | заполнены человеком в форме |

Тариф соответствует карточке, на которой нажали кнопку, с учётом
выбранного режима цены и числа SIM. Привязка к менеджеру работает.

---

## Что сделано по ТЗ

| Пункт ТЗ | Состояние |
|---|---|
| п.1 Лендинг партнёра | готов |
| п.2 Привязка заявки к дилеру | **работает, проверено заявкой в CRM** |
| п.3 Плашка «официальный партнёр» | в подвале, контраст 7.47 |
| п.3 Фирменный стиль КТ | применён по гайду |
| п.3 Адаптивность | проверена на 9 размерах экрана |
| п.3 Поддомен `*.telecom.kz` | **ждём от КТ** |
| п.4 Четыре тарифа | все, цены сверены с ТЗ |
| п.5 Персональные ссылки | `?manager=КОД`, плюс старый формат `/d/КОД` |
| п.6 Форма заявки | форма Qbox, встроена в страницу |
| п.7 Учёт и отчётность | **снято с проекта** — на стороне Qbox |
| п.8 Минимальный результат | всё, кроме домена |

### Изменения объёма относительно исходного ТЗ

Заказчик отменил три вещи, которые в ТЗ были:

- **База данных и бэкенд не нужны** — КТ выделит сервер, заявки идут в Qbox.
- **Админка не нужна** — её роль выполняет Qbox.
- **Учёт продаж по дилерам** — тоже на стороне Qbox.

Позже к ним добавилась четвёртая: **своя форма заявки тоже не нужна**.

Весь этот код написан и работал. Бэкенд и админка лежат в `_archive/`.
Наша форма (`LeadForm.tsx`, `lib/api.ts`, `lib/config.ts`) осталась в
`src/`, но её никто не импортирует — из сборки она выпала сама, и JS
похудел на 12 КБ. Не убрана в архив намеренно: вопрос «чью форму
дорабатывать» до конца не закрыт. Подробности — в [`HANDOFF.md`](HANDOFF.md).

---

## Что на странице

Сверху вниз: плавающая шапка (RU/KZ + кнопка к форме, появляется после
баннера) → полноэкранный баннер → четыре карточки тарифов с переключателем
«с контрактом / без» → «Как подключить» из трёх шагов → нижний CTA-баннер
с кнопкой → подвал. Отдельная страница `/privacy`.

У карточки Bereket есть второй переключатель — **2 SIM / 4 SIM**. Он не
украшение: в Qbox это разные продукты с разными ценами, и без него лендинг
не мог сообщить форме, какой из них выбрали.

Кнопок, ведущих к форме, семь: по одной в шапке, в верхнем баннере и в
нижнем баннере, плюс четыре на карточках тарифов. С карточек уходит
конкретный тариф, с остальных трёх — заявка без тарифа.

Два языка: русский и казахский, переключаются целиком.

---

## Чего не хватает для запуска

1. **Согласие на обработку персональных данных** в форме Qbox. Сейчас в ней
   три поля: Имя, Номер, Город. По закону РК согласие обязательно, а наша
   форма с согласием и версией текста из цепочки ушла. Закрыть может только
   Qbox — это главный блокер.
2. **Домен и сервер от Казахтелекома.** Встраивание разрешено на
   `partner.telecom.kz` — он уже в списке доверенных доменов Qbox.
3. **Живые коды менеджеров.** Проверено на `test`; нужен список реальных.
4. **Юридическая проверка** текста политики на `/privacy` и его перевод на
   казахский.
5. **Лицензия на Helvetica Neue** для веб-использования.

Полный список — в [`TODO.md`](TODO.md).

---

## Технические показатели

Сборка: **271 КБ JS** (86 КБ в gzip) и **33 КБ CSS** (7,6 КБ). Картинки:
баннер 92 КБ, нижний баннер 76 КБ, три иконки шагов по ~100 КБ, шрифты
156 КБ на четыре начертания.

Проверки: типы чистые, 7 из 7 тестов проходят, сборка собирается.
Горизонтальной прокрутки нет на 320/375/768/1024/1440 **в русской версии**;
в казахской при 320px есть переполнение 27px — см. `TODO.md`. Контраст
текста замерен расчётом по отрисованным пикселям, не на глаз.
